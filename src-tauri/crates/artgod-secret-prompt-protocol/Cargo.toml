[package]
name = "artgod-secret-prompt-protocol"
version = "0.0.1"
edition = "2024"
license = "AGPL-3.0-only"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zeroize = { version = "1.8", features = ["alloc", "serde"] }
