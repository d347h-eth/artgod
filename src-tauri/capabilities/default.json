{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "grants the Admin WebView core application APIs only",
    "windows": ["main"],
    "permissions": ["core:default"]
}
