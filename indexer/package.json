{
  "name": "@artgod/indexer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Indexer: No-op indexer ready on port 42069' && node -e 'console.log(\"Indexer ready\"); process.stdin.resume()'",
    "start": "ponder start",
    "codegen": "ponder codegen"
  },
  "dependencies": {
    "@ponder/core": "^0.6.0",
    "hono": "^4.8.4",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
