// This endpoint contract keeps the maintained Admin observability harness on one cataloged route.
export const CONFIG_OBSERVABILITY_HARNESS = Object.freeze({
	host: '127.0.0.1',
	port: 42708,
	routePath: '/e2e-harness/admin/config-observability'
});
