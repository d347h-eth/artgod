import type { ApiBootstrapRun } from './api-types';

/** Default metadata completion mode submitted by the normal bootstrap UI. */
export const DEFAULT_BOOTSTRAP_METADATA_MODE: ApiBootstrapRun['metadataMode'] = 'best_effort';
